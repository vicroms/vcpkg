diff --git a/CMakeLists.txt b/CMakeLists.txt
index 528a485..93694da 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -296,9 +296,7 @@ if(NOT SUPPORT_WGPU_BACKEND)
     set(DAWN_USE_GLFW OFF)
 endif()
 if(SUPPORT_GLFW)
-    set(GLFW_BUILD_WAYLAND ${RAYGPU_USE_WAYLAND})
-    set(GLFW_BUILD_X11 ${RAYGPU_USE_X11})
-    add_subdirectory("amalgamation/glfw-3.4")
+    find_package(glfw3 CONFIG REQUIRED)
 endif()
 
 
@@ -358,7 +356,8 @@ else()
     add_library(${raygpu_core_library_name} STATIC ${RG_SOURCES})
     target_compile_definitions(${raygpu_core_library_name} PUBLIC RG_STATIC=1)
 endif()
-target_include_directories(${raygpu_core_library_name} PRIVATE ${CMAKE_CURRENT_LIST_DIR}/amalgamation/SPIRV-Reflect)
+find_package(unofficial-spirv-reflect CONFIG REQUIRED)
+target_link_libraries(${raygpu_core_library_name} PRIVATE unofficial::spirv-reflect)
 if(RAYGPU_USE_X11)
     target_compile_definitions(${raygpu_core_library_name} PUBLIC RAYGPU_USE_X11=1)
     target_link_libraries(${raygpu_core_library_name} PUBLIC X11::X11)
@@ -446,7 +445,6 @@ else()
         list(APPEND raygpu_linked_deps glslang)
     endif()
     if(SUPPORT_VULKAN_BACKEND)
-        target_include_directories(${raygpu_core_library_name} PRIVATE "amalgamation/vulkan_headers/include")
         list(APPEND raygpu_linked_deps wgvk::wgvk)
     else()
         list(APPEND raygpu_linked_deps webgpu_dawn)
diff --git a/src/backend_wgpu.c b/src/backend_wgpu.c
index 8c48b02..0ed00f8 100644
--- a/src/backend_wgpu.c
+++ b/src/backend_wgpu.c
@@ -11,11 +11,7 @@
 #include "internal_include/internals.h"
 #include "internal_include/wgpustate.inc"
 #define Matrix spvMatrix
-#if SUPPORT_WGPU_BACKEND == 1
-    #include "../amalgamation/SPIRV-Reflect/spirv_reflect.c"
-#else
-    #include "../amalgamation/SPIRV-Reflect/spirv_reflect.h"
-#endif
+#include <spirv_reflect.h>
 #undef Matrix
 wgpustate g_wgpustate = {0};
 
diff --git a/src/cgltf_impl.c b/src/cgltf_impl.c
index 66337fe..1695212 100644
--- a/src/cgltf_impl.c
+++ b/src/cgltf_impl.c
@@ -1,6 +1,6 @@
 // begin file src/cgltf_impl.c
 #define CGLTF_IMPLEMENTATION
-#include <external/cgltf.h>
+#include <cgltf.h>
 
 
 
diff --git a/src/models.c b/src/models.c
index c4c0f72..4864248 100644
--- a/src/models.c
+++ b/src/models.c
@@ -19,9 +19,9 @@
 #endif
 #define TINYOBJ_LOADER_C_IMPLEMENTATION
 #include <external/tinyobj_loader_c.h>
-#include <external/cgltf.h>
+#include <cgltf.h>
 #define PAR_SHAPES_IMPLEMENTATION
-#include <external/par_shapes.h>
+#include <par_shapes.h>
 #ifndef MAX_MATERIAL_MAPS
     #define MAX_MATERIAL_MAPS       12    // Maximum number of maps supported
 #endif
diff --git a/src/msf_gif_impl.c b/src/msf_gif_impl.c
index d08ed64..9c18e60 100644
--- a/src/msf_gif_impl.c
+++ b/src/msf_gif_impl.c
@@ -1,6 +1,6 @@
 // begin file src/msf_gif_impl.c
 #define MSF_GIF_IMPL
-#include <external/msf_gif.h>
+#include <msf_gif.h>
 
 
 
diff --git a/src/raygpu.c b/src/raygpu.c
index c96f220..369d4b0 100644
--- a/src/raygpu.c
+++ b/src/raygpu.c
@@ -88,12 +88,12 @@
 
 
 
-#include <external/stb_image_write.h>
-#include <external/stb_image.h>
-#include <external/sinfl.h>
-#include <external/sdefl.h>
+#include <stb_image_write.h>
+#include <stb_image.h>
+#include <sinfl.h>
+#include <sdefl.h>
 #include "internal_include/internals.h"
-#include <external/msf_gif.h>
+#include <msf_gif.h>
 void ToggleFullscreenImpl(cwoid);
 #ifdef __EMSCRIPTEN__
 #endif  // __EMSCRIPTEN__
diff --git a/src/rtext.c b/src/rtext.c
index a65f0e7..b1079b8 100644
--- a/src/rtext.c
+++ b/src/rtext.c
@@ -86,7 +86,7 @@
     #endif
 
     #define STB_RECT_PACK_IMPLEMENTATION
-    #include <external/stb_rect_pack.h>     // Required for: ttf/bdf font rectangles packaging
+    #include <stb_rect_pack.h>     // Required for: ttf/bdf font rectangles packaging
 
     #include <math.h>   // Required for: ttf/bdf font rectangles packaging
 
@@ -103,7 +103,7 @@
 
     #define STBTT_STATIC
     #define STB_TRUETYPE_IMPLEMENTATION
-    #include <external/stb_truetype.h>      // Required for: ttf font data reading
+    #include <stb_truetype.h>      // Required for: ttf font data reading
 
     #if defined(__GNUC__) // GCC and Clang
         #pragma GCC diagnostic pop
diff --git a/src/sinfl_impl.c b/src/sinfl_impl.c
index 66a3e87..20bf32a 100644
--- a/src/sinfl_impl.c
+++ b/src/sinfl_impl.c
@@ -1,8 +1,8 @@
 // begin file src/sinfl_impl.c
 #define SINFL_IMPLEMENTATION
 #define SDEFL_IMPLEMENTATION
-#include <external/sinfl.h>
-#include <external/sdefl.h>
+#include <sinfl.h>
+#include <sdefl.h>
 
 
 
diff --git a/src/stb_impl.c b/src/stb_impl.c
index e7f5d79..46f54c4 100644
--- a/src/stb_impl.c
+++ b/src/stb_impl.c
@@ -6,8 +6,8 @@
 #pragma GCC diagnostic ignored "-Wextra"
 
 #include <raygpu.h>
-#include <external/stb_image.h>
-#include <external/stb_image_write.h>
+#include <stb_image.h>
+#include <stb_image_write.h>
 
 
 
